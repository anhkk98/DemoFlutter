import 'package:flutter/material.dart';

class Bienchidan extends StatefulWidget {
  @override
  _BienchidanApp createState() => new _BienchidanApp();
}
class _BienchidanApp extends State<Bienchidan> {
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Scaffold(
      appBar: AppBar(
        title: Center(
          child: Text("Biển chỉ dẫn"),
        ),
      ),
      body: Center(
        child: Text("Biển chỉ dẫn"),
    ));
  }
}